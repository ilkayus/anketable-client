export enum HomepageButtonValues {
  CREATE_POLL = "Create New Poll!",
  JOIN_POLL = "Join Existing Poll!",
}

export enum HomepageButtonLinks {
  CREATE_POLL = "createPoll",
  JOIN_POLL = "joinPoll",
}

export const HomepageHeaderText = "Wellcome to Pollable!";
