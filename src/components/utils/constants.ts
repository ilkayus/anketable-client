export enum LinkButtonTitles {
  CREATE_POLL = 'Create New Poll!',
  JOIN_POLL = 'Join Existing Poll!',
  START_OVER = 'Start Over',
  CREATE_BUTTON = 'Create',
  JOIN_BUTTON = 'Join',
  GOTO_LAST_BUTTON = 'Go To Last Poll',
  START_VOTING = 'Start Voting',
  LEAVE_POLL = 'Leave Poll',
  CANCEL_POLL = 'Cancel Poll',
  SUBMIT_VOTES = 'Submit Votes',
}

export enum PageLinks {
  HOMEPAGE = '',
  CREATE_POLL_PAGE = 'createPoll',
  JOIN_POLL_PAGE = 'joinPoll',
  WAITING_ROOM = 'waitingRoom',
  ERROR_PAGE = 'error',
}

export type ButtonColors = 'orange' | 'purple' | 'blue' | 'red' | 'green';
